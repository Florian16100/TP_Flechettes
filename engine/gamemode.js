class Gamemode {
    constructor(players) {
        this.players = players;
        currentPlayer.Index: RandomSource()
        if(status = false)
            console.log ("La partie est terminée !")
    }
}